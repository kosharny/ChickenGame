1. 📖 Project Overview

ChickenGame is a simple arcade game implemented using the SwiftUI framework and the MVVM pattern, where the player taps on spawning eggs to score points until time runs out or lives run out.

2. 🏗️ Architecture and Structure

The project is developed using MVVM (Model-View-ViewModel).

3. 🚀 Run Instructions

The project is ready to compile and run immediately.

Cloning: Clone the repository:

Bash
git clone https://github.com/kosharny/ChickenGame
Opening: Open the ChickenGame.xcodeproj project file in Xcode.

Running: Select the simulator (iOS 16.0+ recommended) and press Run (⌘R).

Requirements:
Target OS: iOS 16.0+
Dependencies: None (pure Swift/SwiftUI)
